open each folder in seperate terminal

1. to run frontend
```npm install```
then
```npm start```

2. to run backend
```npm install```
then
```nodemon start```
if error occurs as nodemon is not defined
```npm uninstall nodemon```
```npm i -g --save-dev nodemon```

